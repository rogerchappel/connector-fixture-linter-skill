{
  "name": "connector-fixture-linter-skill",
  "version": "0.1.0",
  "description": "Local-first agent skill for linting connector action fixtures before rehearsal or release.",
  "type": "module",
  "bin": {
    "connector-fixture-lint": "./bin/connector-fixture-lint.js"
  },
  "files": [
    "bin",
    "src",
    "docs",
    "SKILL.md",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/connector-fixture-lint.js && node --check src/*.js && node --check test/*.test.js",
    "build": "node scripts/build-check.mjs",
    "smoke": "node bin/connector-fixture-lint.js test/fixtures/good --format markdown"
  },
  "keywords": [
    "agent-skill",
    "connector",
    "fixtures",
    "dry-run",
    "approval"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
